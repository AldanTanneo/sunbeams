# Sunbeams: a toy CPU raytracer in Ada

![](cover.png)

### Usage

`./sunbeams <JSON scene>`

Produces `test.png`. Parallelism can be controlled via the `SUNBEAMS_THREADS` environment variable (by default, uses the available amount of logical cores).